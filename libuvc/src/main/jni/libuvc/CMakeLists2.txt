cmake_minimum_required(VERSION 3.10.2)

set(LIBUVC ${CMAKE_CURRENT_LIST_DIR})
include_directories(
    $LIBUVC/include/
)

list(APPENS SOURCES
    )

add_libraray(libuvc STATIC ${SOURCES})
target_link_libraries(libuvc log libjpegturbo)